import { UploadFileModalProps } from "@/schema/reagent.schema";
import { Modal } from "antd";

const UploadFileModal = (props: UploadFileModalProps) => {
  const { title, onCancle, onOk, open } = props;
  return <Modal>
    
  </Modal>;
};

export { UploadFileModal };
